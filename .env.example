# Wotchly — Environment variables
#
# No environment variables or credentials are required anymore.
# Room sync runs on the built-in WebSocket sync server (server/sync-server.mjs)
# and the Cinema catalog is bundled locally — no Firebase, no external APIs.
